# بازطراحی بصری SilvershopIR

بازسازی پایه‌ی بصری سایت با حفظ کاملِ برند، محصولات، متن‌ها و قابلیت‌های موجود.
هیچ محصولی، لینک واتساپی، عکسی یا بخشی حذف نشده است.

---

## فاز ۱ — پایه و سیستم طراحی

### نتیجه‌ی بازبینی (مشکلاتی که پیدا شد)

| مشکل | وضعیت قبل |
|---|---|
| نبودِ پله‌ی فاصله | اعدادِ سرخود: `22px`، `6px`، `.55rem`، `2.2rem`، `3vw`… |
| نبودِ پله‌ی اندازه‌ی متن | ۱۵ اندازه‌ی تقریباً یکسان بین `.68rem` تا `1.15rem` |
| ناهماهنگیِ رنگ | باقی‌مانده‌ی تم‌های قبلی: اسکریمِ **سبز** `rgba(3,8,6)`، فوکوسِ **سبز** `rgba(11,21,18)`، ورودیِ **قهوه‌ای** `rgba(22,19,17)`، هدرِ **گرم** `rgba(13,12,11)` — در تمی که قرار بود تک‌رنگِ ذغالی باشد |
| نبودِ توکنِ شعاع و سایه | `0`/`2px`/`4px` بی‌قاعده؛ سایه‌ها همه یک‌بارمصرف |
| عرضِ ظرف‌های ناهماهنگ | `1500px`، `1200px`، `1100px`، `900px`، `640px`، `38vw` |
| `line-height: 1.9` سراسری | برای برچسب‌ها و متن‌های ریز، بیش‌ازحد باز |
| `font-size: 16px` ثابت روی body | تنظیمِ اندازه‌ی فونتِ مرورگرِ کاربر را نادیده می‌گرفت |
| CSS مرده | `.section-figure` بعد از حذفِ آن بخش باقی مانده بود؛ `.section-body` دوبار تعریف شده بود |
| CLS | ۱۲ عکس از ۱۵ عکس بدون `width`/`height` |
| SEO | بدون Open Graph، بدون داده‌ی ساخت‌یافته، بدون canonical |

### کارهای انجام‌شده

**`css/tokens.css` (فایل تازه)** — تنها منبعِ حقیقت:
- **سطوح**: ۴ پله (`--surface-0` تا `--surface-3`) + شیشه‌ای + اسکریم
- **متن**: ۴ پله با کنتراست سنجیده (`--text-secondary` از `#8F8F8C` به `#A5A5A1` روشن‌تر شد → ۷٫۴:۱)
- **خط**: سه پله (`subtle`/`default`/`strong`) به‌جای یک مقدارِ ثابت و یک `rgba` سرخود
- **تایپوگرافی**: ۱۰ پله‌ی اندازه، ۶ ارتفاعِ خط، ۶ فاصله‌ی حروف
- **فاصله**: پله‌ی ۸ واحدی (`--space-1` تا `--space-12`) + ریتمِ بخش + ناودانِ صفحه
- **ظرف**: ۵ عرضِ استاندارد
- **شعاع و سایه**: توکن‌های مشخص؛ سایه‌ها عمق‌محور، متناسب با تمِ تیره
- **حرکت**: ۵ مدت‌زمان (۱۸۰ تا ۶۰۰ms) + ۳ منحنیِ شتاب
- **لایه‌بندی**: ۸ توکنِ `z-index` به‌جای اعدادِ پراکنده

**`css/style.css` (بازنویسیِ کامل، ۷۰۳ → ۹۹۰ خط، همه‌ی نام‌کلاس‌ها دست‌نخورده)**
- هر مقدار از توکن می‌آید. تنها استثناها: `-1px` الگویِ استانداردِ پنهان‌سازی، و یک clamp اختصاصی برای واژه‌نشانِ برند که نباید بشکند.
- ناهماهنگیِ رنگ کامل برطرف شد — حالا واقعاً تک‌رنگ است.
- تیترها از وزن **۹۰۰ به ۷۰۰** و تراکینگِ منفی رسیدند؛ حسِ ادیتوریال به‌جای وبِ پررنگ.
- وزنِ متنِ بدنه از **۳۰۰ به ۴۰۰** رفت — خوانایی در سایزهای کوچک.
- CSS مرده حذف شد.

**امضای بصری — «خطِ درخشان»**
یک هایرلاینِ گرادیانی (`--rule-specular`) که مثل بازتابِ نور روی لبه‌ی نقره‌ی صیقلی از سمتِ خواندن روشن می‌شود و محو می‌شود. جایگزینِ خطِ صافِ معمولی زیرِ مشخصاتِ هیرو و بالای هر گالری. از جنسِ خودِ محصول گرفته شده، نه تزئینِ عمومی.

**دکمه‌ها** — یک پایه، سه گونه (`solid` / `ghost` / `is-compact`) با متغیرهای محلی. قبلاً `.pcard-cta` پدینگ و ارتفاعِ دکمه را دوباره تعریف می‌کرد؛ حالا فقط توکن را عوض می‌کند.

**کارت‌ها** — پدینگِ یکدست (`--space-6`)، خطِ ظریف‌تر، سایه‌ی تمیز، جداکننده‌های ممتد به‌جای نقطه‌چین.

---

## فاز ۲ — حرکت

فقط `transform` و `opacity`؛ بدونِ جابه‌جاییِ چیدمان، بدون فنر و پرش.

- **ظاهرشدنِ تدریجی** — `IntersectionObserver` روی گالری‌ها با تأخیرِ پلکانی (۶۰ms برای تیتر تا ۴۵۰ms برای کارتِ سوم). یک‌بار اجرا و رها می‌شود.
- **دکمه‌ی مغناطیسی** — تا ۶px به سمتِ نشانگر کشیده می‌شود.
- **فشردنِ دکمه** — `scale(.985)` در ۱۸۰ms.
- **عمقِ تصویر** — عکسِ کارت خلافِ جهتِ نشانگر تا ۱۰px می‌لغزد و با بزرگ‌نماییِ هاور ترکیب می‌شود.
- **ترکیبِ ترنسفورم‌ها** — هاور، فشردن، مغناطیس و لغزش همگی روی متغیرهای CSS می‌نشینند تا یک `transform` واحد بماند و همدیگر را خنثی نکنند.
- روی دستگاهِ لمسی و در حالتِ «کاهش حرکت»، هیچ‌کدام فعال نمی‌شوند.

### یک باگِ واقعی که پیدا و رفع شد

دکمه‌ی «استعلام موجودی» در هر سه بخشِ سینمایی **۸۰ پیکسل بیرونِ لبه‌ی راست گیر کرده بود** و برش می‌خورد. علت: `gsap.from()` مقدارِ پایانی را از «حالتِ عنصر هنگامِ ساخت» برمی‌داشت و برای آخرین عنصرِ پلکانی درست حل نمی‌شد. با `fromTo` و نوشتنِ صریحِ مقصد رفع شد. این باگ از قبل وجود داشت (با تست روی کامیتِ قبلی تأیید شد) و چون `overflow-x: clip` جلوی اسکرولِ افقی را می‌گرفت، دیده نشده بود.

همان اصلاح، `switch` هفت‌شاخه‌ی انیمیشن را به یک جدولِ داده‌ای تبدیل کرد.

---

## فاز ۳ — ارائه‌ی محصول

- **گالریِ نیم‌ست‌ها** کادرِ عمودی ۳:۴ گرفت تا از گوشواره تا آویز در قاب بماند.
- **`auto-fit` به‌جای `auto-fill`** — قبلاً یک ستونِ خالی جا می‌گرفت و سه کارت، ردیف را پر نمی‌کردند و یک‌سومِ عرض هدر می‌رفت.
- **هاله‌ی نورِ ویترین** پشتِ محصولِ هیرو (`--glow-ambient` + گرادیانِ شعاعیِ اختصاصی).
- **بزرگ‌نمایی در نمایشگر عکس** — کلیک روی عکس آن را ۱٫۹ برابر می‌کند و قاب با حرکتِ نشانگر جابه‌جا می‌شود.
- **محوِ متقابل بینِ عکس‌ها** — عکسِ بعدی پیش از نمایش آماده می‌شود تا پرش نداشته باشد.
- **بومِ روشنِ محصول** با سایه‌ی عمیق‌تر، شبیهِ قابِ ویترین.

---

## فاز ۴ — پرداخت، دسترس‌پذیری، سرعت، SEO

**دسترس‌پذیری**
- لینکِ «رفتن به محصولات» برای کاربرِ کیبورد.
- کنتراستِ متنِ ثانویه به ۷٫۴:۱ رسید (پیش‌تر ۶٫۰:۱).
- همه‌ی هدف‌های لمسی ≥ ۴۴px و همه‌ی متن‌ها ≥ ۱۱px — با تست در چهار اندازه‌ی صفحه تأیید شد.
- `scroll-margin-top` روی هدف‌های ناوبری تا تیتر زیرِ هدرِ ثابت پنهان نشود.
- بدونِ جاوااسکریپت، عکس‌ها و متن‌ها دیده می‌شوند (پنهان‌سازی به کلاسِ `js` مشروط شد).
- `prefers-reduced-motion` همه‌ی حرکت‌ها را خاموش می‌کند.

**سرعت**
- `width`/`height` روی هر ۱۴ عکسِ ثابت → CLS نزدیک صفر.
- `preload` برای دو فونتِ بالای صفحه و عکسِ هیرو با `fetchpriority="high"` → LCP سریع‌تر.
- `will-change` فقط روی عناصری که واقعاً متحرک‌اند.

**SEO**
- Open Graph و Twitter Card کامل.
- داده‌ی ساخت‌یافته‌ی `JewelryStore` با نشانی، تلفن و شبکه‌های اجتماعی.
- `canonical` و `theme-color`.

**یک اصلاحِ چیدمان**
شماره‌ی تلفن (لاتین) در بخشِ تماس به لبه‌ی چپِ خانه چسبیده بود، نه زیرِ عنوانِ خودش. علت: `inline-flex` در ستونِ راست‌به‌چپ بلوکی می‌شد و `direction: ltr` مبدأ را برمی‌گرداند.

---

## وردپرس

تمِ وردپرس همان `css/tokens.css` و `css/style.css` را می‌خواند، پس تمامِ این تغییرات آنجا هم اعمال می‌شود. `functions.php` به‌روز شد تا لایه‌ی توکن پیش از استایلِ اصلی بارگذاری شود.

---

## آنچه بررسی شد

چهار اندازه‌ی صفحه (۱۴۴۰ / ۸۳۴ / ۳۹۰ / ۳۶۰): بدونِ سرریزِ افقی، بدونِ خطای جاوااسکریپت، همه‌ی هفت بخشِ سینمایی در بازه‌ی خودشان کامل ظاهر می‌شوند و هیچ عنصری از قاب بیرون نمی‌زند. نمایشگر عکس، کلیدهای جهت‌دارِ راست‌به‌چپ، نقطه‌های نشانگر، قفلِ اسکرولِ پس‌زمینه و بازگشتِ فوکوس — همه تست شدند.

---

## جهت‌گیریِ تازه — مشکیِ گرم و طلا (بر پایه‌ی نمونه‌ی Aurel)

سایت از تکِ‌رنگِ ذغالی به **مشکیِ گرم با اکسنتِ طلایی** تغییر کرد. چون همه‌چیز روی
لایه‌ی توکن نشسته بود، تغییرِ کلِ پالت با ویرایشِ یک فایل انجام شد.

**رنگ**
- سطوح از خنثیِ خاکستری به گرم رفتند: `#0D0D0F` → `#0B0A09` و هم‌خانواده‌هایش.
- طلا در سه پله (`--gold`, `--gold-bright`, `--gold-dim`) اضافه شد.
- خط‌ها طلاییِ کم‌رنگ شدند — همان لبه‌ی نازکی که در نمونه دورِ هر قاب دیده می‌شود.
- همه‌ی اسکریم‌ها و سایه‌ها هم‌دمای پس‌زمینه شدند تا جایی آبی‌زده نماند.
- **طلا فقط برای لبه، برچسب و مقدار** — هیچ سطحِ بزرگی طلایی نشد، تا نقره‌ی خودِ
  محصول تنها درخششِ صفحه بماند.

**طاق — امضای تازه‌ی بصری**
قابِ محصول از مستطیل به **طاق** تغییر کرد: بالای کادر گرد می‌شود، با لبه‌ی نازکِ
طلایی. همان عنصری که در نمونه بیشترین نقش را در «لوکس دیده‌شدن» دارد، و اینجا با
طاقِ اصفهانی هم هم‌معناست. کارت دیگر جعبه ندارد — خودِ طاق مرزِ بصری است.

**تایپوگرافی**
- دو وزنِ نازکِ وزیرمتن (Thin ۱۰۰ و ExtraLight ۲۰۰) اضافه شد.
- تیترها از وزن ۷۰۰ به **۲۰۰** رفتند و اعدادِ آمار به **۱۰۰**. ظرافت از نازکیِ
  قلم می‌آید، نه از ضخامتش.
- برچسبِ بالای هر بخش طلایی شد و خطِ کوتاهِ محوشونده کنارش نشست؛ در گالری‌ها
  وسط‌چین با خط در هر دو طرف — همان الگوی سرصفحه‌ی نمونه.

**کارت محصول**
مثل نمونه: طاق ← نامِ سبکِ وسط‌چین ← مشخصات با مقدارِ طلایی ← دکمه‌ی خطیِ طلایی.

**دکمه‌ها** پر و خطی، هر دو طلایی با فاصله‌ی حروفِ بازتر.

**دو اصلاحِ ریسپانسیو در همین مسیر**
- مقدارهای `nowrap` مشخصات در کارتِ ۱۶۳ پیکسلیِ موبایل سرریز می‌کردند.
- گالریِ موبایل از دو ستون به **یک ستون** رفت؛ دو ستون هم عکس و هم متن را خفه
  می‌کرد و متنِ دکمه دو خط می‌شد. نمونه هم روی موبایل یک محصول در هر ردیف دارد.

---

## پیشنهاد برای بعد

1. **فونت کلمه** — لایسنسش را دارید؛ فایل‌ها را در `assets/fonts/kalameh/` بگذارید. الان به وزیرمتن برمی‌گردد و ۴ درخواستِ ۴۰۴ می‌خورد.
2. **عکسِ محصولاتِ گالریِ انگشترها** — هنوز SVGهای جای‌گیرند. عکسِ واقعی بفرستید تا مثل نیم‌ست‌ها جایگزین شوند.
3. **اعدادِ بخشِ آمار** — هنوز نمونه‌اند (در `index.html` با `TODO` علامت خورده‌اند).
4. **`srcset`** برای عکس‌های محصول، وقتی تعدادشان بیشتر شد.
5. **دامنه‌ی واقعی** — بعد از اتصال `silvershopir.com`، آدرس‌های `canonical` و Open Graph باید به آن تغییر کنند.
